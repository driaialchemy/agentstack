# CLAUDE.md — Agent Policy

> Draft generated by AIAlchemy Repo Governor for human review.
> Paste this file at the repository root. Do not commit until you have read it.

## Project Purpose

Governed multi-agent intelligence stack used as portfolio proof. Runs on synthetic data. Operating rule: no charter, no run. See `AGENT_CHARTER.md` and `ARCHITECTURE.md`.

**Repository:** `agentstack`
**Risk Level:** MEDIUM

## Allowed Operations

- Read charter, architecture, and existing agent/skill/governance code.
- Run tests if present.
- Edit a single phase area at a time (`agents/`, `skills/`, `governance/`, `orchestration/`).

## Forbidden Operations

- Do **not** delete files without explicit user confirmation.
- Do **not** commit or push changes automatically.
- Do **not** read, print, or log the contents of `.env`, secrets, or credential files.
- Do **not** install packages outside a virtual environment without asking.
- Do **not** run destructive commands (`rm -rf`, `git reset --hard`, `DROP TABLE`, etc.).
- Do **not** point agents at live customer data.
- Do **not** bypass charter or gate checks to 'just make it run'.
- This repo is large (600+ files). Do not do repo-wide refactors.

## Blocked Paths (never read contents)

- `.env`, `.env.*`, `.env.local`, `.env.production`
- `secrets.*`, `credentials.*`, `*.pem`, `*.key`
- Any file under `secrets/`, `private/`, or `credentials/`

## Environment

- **Language(s):** Python
- **Build / install:** requirements.txt

## In-scope edit areas

Prefer small edits in existing project folders. Stay out of `.git/`, virtualenvs, and secret files.

## Notes

- Bind every workflow to a completed charter and accountability owner.
- Prefer synthetic data already in the repo.
- Scope changes narrowly; ask before touching more than one top-level package.

## How to verify

```bash
pytest
```

Always run the verification command before claiming a task is complete.
